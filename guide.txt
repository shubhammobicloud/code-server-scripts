* List only normal (human) users
awk -F: '$3>=1000 {print $1}' /etc/passwd

* Delete the test user first
./delete-user.sh
//sudo pkill -9 -u dev1
//sudo userdel -r dev1


cd ~/code-server-scripts
chmod +x create-user.sh setup-code-server.sh delete-user.sh git-setup.sh


./create-user.sh dev1
./setup-code-server.sh dev1 8081
systemctl status code-server@dev1
* Logs can be checked with:
journalctl -u code-server@dev1 -f


*Logs
journalctl -u code-server@devuser -n 20


* Deletes the per-user git wrapper binary:
rm -f /usr/local/bin/git-$USERNAME
*Removes the git alias from the user’s .bashrc:
sed -i "/alias git=.*git-$USERNAME/d" /home/$USERNAME/.bashrc
